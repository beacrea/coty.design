name: Fix Commit Author

on:
  push:
    branches: [main]

permissions:
  contents: write

jobs:
  fix-author:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Check and fix Replit-authored commits
        env:
          BEFORE_SHA: ${{ github.event.before }}
          AFTER_SHA: ${{ github.sha }}
        run: |
          WRONG_EMAIL="51382616-beasles@users.noreply.replit.com"
          CORRECT_NAME="Coty Beasley"
          CORRECT_EMAIL="beacrea@users.noreply.github.com"

          if [ "$BEFORE_SHA" = "0000000000000000000000000000000000000000" ]; then
            RANGE="$AFTER_SHA"
          else
            RANGE="$BEFORE_SHA..$AFTER_SHA"
          fi

          BAD_COUNT=$(git log --format='%ae' "$RANGE" | grep -c "$WRONG_EMAIL" || true)

          if [ "$BAD_COUNT" -eq 0 ]; then
            echo "All commits in push range have correct author. No action needed."
            exit 0
          fi

          echo "Found $BAD_COUNT commit(s) with incorrect Replit author in range $RANGE. Fixing..."

          git config user.name "$CORRECT_NAME"
          git config user.email "$CORRECT_EMAIL"

          FILTER_BRANCH_SQUELCH_WARNING=1 git filter-branch -f --env-filter "
            if [ \"\$GIT_AUTHOR_EMAIL\" = \"$WRONG_EMAIL\" ]; then
              export GIT_AUTHOR_NAME=\"$CORRECT_NAME\"
              export GIT_AUTHOR_EMAIL=\"$CORRECT_EMAIL\"
              export GIT_COMMITTER_NAME=\"$CORRECT_NAME\"
              export GIT_COMMITTER_EMAIL=\"$CORRECT_EMAIL\"
            fi
          " -- "$RANGE"

          git push --force-with-lease origin main
          echo "Fixed $BAD_COUNT commit(s) with incorrect author."
